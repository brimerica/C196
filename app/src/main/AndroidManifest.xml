<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.C196">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_public_teal"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_public_teal"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddEditMentorActivity" />

        <activity android:name=".AddEditAssessmentActivity" />
        <activity android:name=".MentorActivity" />
        <activity android:name=".AssessmentActivity" />
        <activity android:name=".CourseInfoActivity" />
        <activity android:name=".AddEditCourseActivity" />
        <activity android:name=".AddEditTermActivity" />
        <activity android:name=".CourseActivity" />
        <activity android:name=".TermActivity" />
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver
            android:name=".utilities.MyReceiver"
            android:enabled="true"
            android:exported="true"></receiver>
    </application>

</manifest>